node_modules/

# Android build artifacts (the web shell is synced in by `cap sync`).
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/capacitor-cordova-android-plugins/build/
android/app/src/main/assets/public/

# The JSON store is runtime state, not source. `npm run seed` rebuilds it.
data/*.json
data/*.json.bak
data/*.corrupt-*
data/evidence/
!data/.gitkeep

.DS_Store
*.log
.env
